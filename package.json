{
  "name": "rpgmv-bridge",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "cli": "tsx src/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "lint:fix": "biome check --write src/",
    "format": "biome format --write src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check": "npm run lint && npm run typecheck && npm run test:coverage",
    "generate-fixtures": "tsx scripts/generate-fixtures.ts",
    "http": "tsx src/http/server.ts",
    "electron": "electron electron/main.cjs",
    "fixtures:engine": "node scripts/fetch-engine-fixtures.mjs",
    "pretest": "npm run fixtures:engine",
    "pretest:coverage": "npm run fixtures:engine",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "commander": "^12.0.0",
    "fast-json-patch": "^3.1.1",
    "fastify": "^5.8.5",
    "pino": "^9.0.0",
    "write-file-atomic": "^5.0.1",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^20.0.0",
    "@types/write-file-atomic": "^4.0.3",
    "@vitest/coverage-v8": "^4.1.11",
    "axe-core": "^4.11.1",
    "electron": "^42.3.3",
    "fast-check": "^3.22.0",
    "happy-dom": "^20.14.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  }
}
